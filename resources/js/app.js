import './bootstrap';
import 'bootstrap';
import feather from 'feather-icons';
import.meta.glob([
    '../images/*',
  ]);

  
 
  (function() {
    // your page initialization code here
    // the DOM will be available here
    feather.replace();
 })();